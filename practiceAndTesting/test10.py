# import numpy as np

# a = np.arange(27).reshape((3, 3))
# b = np.arange(27).reshape((3, 3))

# print(np.matmul(a,b))


# from functools import reduce


# inList = range(1,5+1)
# inLista = reduce(lambda x,y: x+y,inList)
# print(inLista)

input_string = input().split(",")
print(input_string)